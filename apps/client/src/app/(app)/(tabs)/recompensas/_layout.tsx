import { Stack } from 'expo-router';

/** Pila de la pestaña Recompensas: formularios e historial con la navegación visible. */
export default function RewardsStackLayout() {
  return <Stack screenOptions={{ headerShown: false }} />;
}
