rules_version = '2';

// Fase 00: todo denegado. Las reglas reales se escriben con TDD en la fase 03 (ver .claude/plan/03-security-rules.md).
service cloud.firestore {
  match /databases/{database}/documents {
    match /{document=**} {
      allow read, write: if false;
    }
  }
}
